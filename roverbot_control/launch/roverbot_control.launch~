<?xml version="1.0"?>
<launch>
	<!-- Turtlesim Node-->
	<node pkg="turtlesim" type="turtlesim_node" name="sim"/>

	<!-- joy node -->
	<node respawn="true" pkg="joy"
	type="joy_node" name="turtle_joy" >
	<param name="dev" type="string" value="/dev/input/js0" />
	<param name="deadzone" value="0.12" />
	</node>

	<!-- Axes -->
	<param name="axis_linear" value="1" type="int"/>
	<param name="axis_angular" value="0" type="int"/>
	<param name="scale_linear" value="2" type="double"/>
	<param name="scale_angular" value="2" type="double"/>
	<node pkg="roverbot_control" type="roverbot_control" name="teleop"/>

	<!-- Load joint controller configurations from YAML file to parameter server 
	<rosparam file="$(find roverbot_control)/config/roverbot_control.yaml" command="load"/>
-->
	<!-- load the controllers 
	<node name="controller_spawner" pkg="controller_manager" type="spawner" respawn="false"
		output="screen" ns="/roverbot" args="joint_state_controller 
							leftFrontTire_effort_controller 
							rightFrontTire_effort_controller 
							leftFrontTire_effort_controller 
							rightFrontTire_effort_controller"/>

-->
	<!-- convert joint states to TF transforms for rviz, etc
	<node name="robot_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher"
		respawn="false" output="screen">
		<param name="robot_description" command="$(find xacro)/xacro.py '$(find roverbot_description)/urdf/roverbot.xacro'" />
		<remap from="/joint_states" to="/roverbot/joint_states" />
	</node>
 -->
</launch>
